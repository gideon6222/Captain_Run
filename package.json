{
  "name": "captain-run",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "node --test test/tuning.test.mjs test/spawn.test.mjs",
    "e2e": "playwright test",
    "size": "node scripts/check-bundle-size.mjs",
    "size:update": "node scripts/check-bundle-size.mjs --update"
  },
  "dependencies": {
    "three": "0.166.0"
  },
  "devDependencies": {
    "@playwright/test": "^1.63.0",
    "esbuild": "^0.25.0",
    "vite": "^5.4.0",
    "vite-plugin-pwa": "^1.3.0"
  }
}
